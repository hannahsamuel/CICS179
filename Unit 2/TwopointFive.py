# A program that outputs the momentum of an object by taking in mass and velocity as inputs
mass = float(input("Mass: "))
velocity = float(input("Velocity: "))
print("The object's momentum is ", mass*velocity)